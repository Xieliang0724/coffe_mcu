# Changelog

## [v1.0.0] - coffe_mcu

> 基于 `esp32c5_web_provision`（v1.2.4）二次开发的新项目 **coffe_mcu**。目标芯片 ESP32-C5 **N16R8**。

### ✨ 新增

- **咖啡台 10 路 LED 独立开关**：`led_control.[ch]`，CH1..CH10 映射 `GPIO 0,1,4,8,13,14,16,17,23,24`；NPN/漏极低边开关，高有效；状态存 NVS（命名空间 `coffee_led`），上电先全灭再恢复。
- **LED REST 接口**：`GET /api/leds`、`POST /api/led`；网页新增「☕ 咖啡台 LED 控制」卡片。
- **Modbus TCP 从站**：`modbus_slave.[ch]`，固定监听端口 **502**，把 10 路 LED 以**保持寄存器** `0x0000..0x0009` 暴露（值 0/1），支持功能码 `0x03/0x06/0x10`；纯网络实现，**不占用任何串口**。

### 🔧 变更 / 移除

- **移除 RTU↔TCP 网关**（原 `modbus_gw`）：本项目无 Modbus RTU 从站，不再走 UART 桥接；**UART1（GPIO5/6）预留给未来的 485 舵机总线**。
- **移除 TLS**：本工程用不到，删除 `mbedtls` 依赖、`main/certs/`（证书/私钥）、`GET /api/cert` 及前端 TLS 配置项。
- **闪存大小**：`CONFIG_ESPTOOLPY_FLASHSIZE_16MB`（N16R8）。
- **兼容 IDF v6**：`wifi_sta_config_t` 已无 `ssid_len`，改为空结尾字符串写入。
- 项目名改为 `coffe_mcu`；HTTPD 路由上限提到 16。

---


### 🔧 修复

- **Modbus 网关停止竞态**：网关停止过程中，监听任务不会再把新接受的连接写入已清理的客户端槽位，避免停止后残留连接占用新实例的槽位。
- **AP 名称拼接越界**：修复 AP 前缀过长时 `snprintf` 返回期望长度导致的缓冲区越界风险。
- **Wi-Fi 自断开标志残留**：进入配网、新连接周期及联网成功时清理旧的主动断开标志，避免影响后续重试计数。
- **配网页面热点提示**：热点关闭时使用最近一次状态中的动态 AP 名称，不再因页面元素显示 `-` 而丢失热点名。

本项目的版本记录。版本号格式：`v主.次.修订`

- **主版本**：重大变更/不兼容改动
- **次版本**：新功能
- **修订版本**：bug 修复/小优化

## [v1.2.3] - 2026-08-22

### ✨ 新增：AP 名称基于芯片 MAC（每台设备唯一）

- AP 名称后缀改用**芯片 MAC（STA 基址，出厂唯一）**的末尾 N 字节（默认 4 字节 = 8 位十六进制，约 40 亿组合）
- 之前只用 MAC 后 2 字节（4 位十六进制，65536 组合），多台设备可能重名；现在实际保证唯一
- 新格式：`ESP32C5-75885C70`（前缀-后8位）；可经 `CONFIG_PROV_AP_SSID_MAC_BYTES` 配置（1~6 字节）
- 顺带修复：前端"断开 Wi-Fi"提示不再硬编码热点名，改为动态显示当前设备 AP 名（README 已知问题清除）

## [v1.2.2] - 2026-08-22

### 🔧 修复（代码审查发现的 4 个高优先级 bug）

- **连接超时重试计数双计**（wifi_mgr）：超时定时器先计数，随后的 `esp_wifi_disconnect()` 又触发 DISCONNECTED 事件再次计数，每次"卡住超时"消耗 2 次重试预算（5 次实际 2~3 次就进配网模式）。改为主动断开打标记，事件侧不重复计数；同时去掉超时路径中与断开竞争的无效 `esp_wifi_connect()`，并保留超时定时器重武装作为看门狗（断开事件异常丢失时状态机仍能周期推进直至回退配网模式）
- **Web 延迟操作内存泄漏**（web_server）：`POST /api/config` 每次泄漏 malloc 的配置副本（约 200B），且 /api/config、/api/reset、/api/disconnect 每次请求泄漏一个 esp_timer 句柄，反复配网缓慢耗尽堆。改为静态定时器复用 + 静态待连接配置（顺带消除了定时器回调的函数指针类型双关）
- **Modbus RTU 响应帧泄漏**（modbus_gw）：响应队列满时丢指针不释放；每次事务开头 `xQueueReset` 丢弃滞留帧指针——从站慢响应（恰超 1s 超时）场景每事务泄漏一帧（最大 262B）。改为逐个取出释放
- **网关停止任务泄漏 + fd 复用误关**（modbus_gw）：阻塞式 `accept()/recv()` 无法被 `close()` 唤醒，每次网页改网关配置最多泄漏 6 个任务；残留客户任务退出时按 fd 号匹配槽位，fd 被复用后可能关掉新连接的 socket。改为 `shutdown()` 先行唤醒 + 客户任务携带槽位、退出时校验 fd 归属；同时修复 TLS 握手失败路径不归还槽位的问题（多次失败后拒绝所有连接）

## [v1.2.1] - 2026-08-16

### 🔧 开发工具

- 新增 `tools/serial_read.py`：串口日志读取脚本（无需 TTY，DSH 会话可直接调用；支持 `--reset` 复位抓启动日志）
- 新增 `tools/serial_monitor.sh`：后台持续抓取串口日志到文件
- 说明：串口访问与 TCC 无关（`/dev/cu.*` 世界可读写）；若某会话报 `Permission denied` 属该会话沙箱未放行，需提升沙箱权限

## [v1.2.0] - 2026-08-16

### ✨ 新增：AP 兜底延迟可配置

- **需求背景**：ESP32-C5 单射频，STA 连接/扫描时 AP 信号弱；且板子无复位按钮，AP 和 STA 绝不能同时失联
- **新逻辑**：STA 断开后不立即开关 AP，而是**连续断开超过 N 秒**（默认 15s）才自动开启 AP 兜底
  - 短暂断网（< N 秒）重连成功 → AP 不动作（避免射频来回跳变）
  - 长时间断开（≥ N 秒）→ 自动开启热点，随时可重新配网
- **网页可配**：配网表单新增"断网后开启热点兜底延迟（秒）"，范围 0~3600，0 = 立即开启，随配置持久化（NVS）

## [v1.1.4] - 2026-08-16

### 🔧 修复

- 联网成功后重置重试计数（避免重试次数跨连接周期累计，导致下次断网重试预算不足）

## [v1.1.3] - 2026-08-16

### 🔴 关键修复：AP/STA 不再同时失联

- **问题**：设备联网成功后（AP 已关闭、纯 STA 模式），一旦 STA 断开，状态机未切回 CONNECTING，导致重试逻辑直接返回 —— 既不重连也不重开 AP，设备永久失联（无复位按钮的板子只能拔电）
- **修复**：
  1. STA 断开时切回 CONNECTING 状态，让重试真正运行
  2. 重试期间保证 AP 可用（兜底不变量："未联网时 AP 必须在"）
  3. 重试耗尽后回退配网模式（AP 开启）
- **保证**：任何时刻 AP 和 STA 至少有一个可用

## [v1.1.2] - 2026-08-16

### 🔧 修复

- TLS 客户端任务栈 6144 → 8192（避免 mbedTLS 握手栈溢出）
- 修复客户端槽位泄漏（malloc/任务创建失败时未归还槽位，多次失败后耗尽导致拒绝所有连接）
- 客户端槽位分配互斥锁（明文/TLS 双监听并发安全）

## [v1.1.1] - 2026-08-16

### ✨ 新增

- **证书下载接口**：`GET /api/cert` 直接下载设备 TLS 证书（PEM），客户端无需从仓库/项目里翻文件，连接设备即可获取并信任

### 🔒 安全

- 私钥 `main/certs/server_key.pem` 移出公开仓库（加入 `.gitignore`），仅保留在本机用于编译

## [v1.1.0] - 2026-08-16

### ✨ 新增：Modbus TCP over TLS（单向验证）

- **TLS 加密监听**：新增 TLS 端口（默认 802，Modbus Security 标准），与明文 502 端口共存
- **单向 TLS**：客户端验证设备身份（服务端证书），设备不验证客户端
- **固件内置证书**：自签名服务器证书（CN=`esp32c5.local`，10 年有效）随固件内置；可通过 `main/certs/` 替换为自己的 CA 证书
- **网页配置**：高级设置新增"启用 TLS + TLS 端口"配置项，与明文端口冲突校验
- **协议一致**：TLS 连接同样走 RTU↔TCP 协议转换、CRC16 校验、IP 白名单、事务串行化

### 🔧 修复

- 修复 mbedTLS 解析嵌入式 PEM 证书失败（缓冲区需 null 结尾）
- 客户端槽位分配增加互斥锁（明文/TLS 双监听并发安全）

## [v1.0.0] - 2026-08-15

> ⚠️ **基础版本说明**：本版本为 **v1.0.0 基础版本**，**Modbus TCP 不带 TLS 验证功能**（明文 TCP，端口 502）。TLS 加密将在后续版本（v1.1.0+）提供；适用于可信局域网，跨公网/不可信网络请使用支持 TLS 的版本。

### ✨ 功能特性

- **Web 配网**：SoftAP 热点 + 网页配置（`http://192.168.4.1`），支持双频（2.4G/5G）扫描、SSID 下拉选择、隐藏网络手动输入
- **IP 配置**：DHCP 自动获取 / 静态 IP（IP、掩码、网关、DNS 可自定义）
- **SoftAP 管理**：可选"联网成功后关闭热点"；网页提供"开启/关闭热点"按钮和"一键进入配网模式"按钮
- **RGB 状态灯**（GPIO27 WS2812）：橙色=未联网、蓝色=有设备连热点、绿色=联网成功
- **Modbus RTU↔TCP 网关**（UART1）：协议转换（MBAP↔RTU+CRC16），支持本地端口、RTU 波特率、UART1 TX/RX GPIO、客户端 IP 白名单配置
- **mDNS**：局域网内可通过 `http://esp32c5.local` 访问
- **网络状态面板**：STA 状态/SSID/IP/网关/掩码/DNS/信号强度 + AP 状态/名称/客户端数/固件版本

### 🛡️ 可靠性设计

- **防失联保护**：SoftAP 仅在联网成功后关闭；连接中/配网模式热点始终可用
- **自动回退**：连接失败自动重试（可配次数），耗尽后回到配网模式
- **配置持久化**：NVS 存储，断电重启自动重连
- **长按 BOOT 复位**：清除配置并重新配网

### 🔧 其他

- 基于 ESP-IDF v6.0.1（ESP32-C5），8MB 闪存（N8R4）
- 固件内置版本号（网页/API/日志可见）
